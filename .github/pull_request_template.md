## Summary

<!-- 1–3 bullets: what changed, why. -->

## Linear

<!-- HER-XXX (or "n/a") -->

## Test plan

- [ ] `swift test` green locally
- [ ] `swiftformat --lint .` clean
- [ ] `swiftlint lint` clean
- [ ] Bruno regenerated (`make bruno-regen`) if `openapi.yaml` touched
- [ ] DTO added to `LuminaVaultShared` first if wire shape changed
