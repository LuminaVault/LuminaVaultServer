.DS_Store
/.build
/.swiftpm
/.devContainer
/Packages
/*.xcodeproj
xcuserdata/
/.vscode/*
!/.vscode/hummingbird.code-snippets
.env.*
!.env.example
.env.worktrees/
.env
data
.build
.build*
# Nested worktrees (Claude Code EnterWorktree + manual git worktree add).
# Each is tracked as a gitlink entry, which clutters `git status` on the
# host repo for every concurrent worktree session.
.claude/worktrees/
secrets
