meta {
  name: MFA Resend
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/v1/auth/mfa/resend
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{testEmail}}"
  }
}

tests {
  test("status 202 accepted", function() {
    expect(res.status).to.equal(202);
  });
}
