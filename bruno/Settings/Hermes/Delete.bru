meta {
  name: Delete Hermes Config
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/v1/settings/hermes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  HER-197 — drop the per-user Hermes endpoint override. Subsequent
  Hermes-backed traffic for this user falls back to the managed
  default. 204 on success, 404 if no override existed.
}
