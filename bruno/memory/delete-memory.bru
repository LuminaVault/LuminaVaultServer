meta {
  name: DELETE memory
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/v1/memory/{{memoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("204 No Content", function() {
    expect(res.status).to.equal(204);
  });
}
