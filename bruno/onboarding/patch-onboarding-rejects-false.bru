meta {
  name: PATCH onboarding rejects false
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/v1/onboarding
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "firstQueryCompleted": false
  }
}

tests {
  test("400 Bad Request", function() {
    expect(res.status).to.equal(400);
  });
}
